# ClockEvent TODO

## Next Project: An interactive map that shows all US interventions in foreign countries by era and type, where you can mouse over each affected country and get both a timeline of US interventions and a link to wikipedia or another reputable source backing up each claim.

## Last Session (2026-03-10)

Bugs to fix:
- [ ] Clock showing 24-hour format with AM/PM (e.g. "0:11 AM" instead of "12:11 AM") — can't verify until midnight

Fixed this session:
- [X] Phantom events — rewrote RRULE expansion (proper week-by-week with INTERVAL, BYDAY, monthly nth-day support)
- [X] DST — occurrence dates now constructed fresh with explicit h/m/s instead of mutating Date with setDate()
- [X] Clock scales to widget width (font size computed to fill available width)
- [X] Removed dead DragHandler/startSystemMove code (desktop applets use Plasma's built-in long-press drag)

In progress:

## Session (2026-03-03)

Progress made:
- [X] Timezone: vertical letters beside AM/PM — WORKS
      - Split time into two labels ("9:53 " + "AM") so we can anchor tz column to ampmLabel
      - Used TextMetrics.tightBoundingRect.height (=34px) for actual glyph height
      - Position: y = ampmLabel.y + (ampmLabel.height - glyphHeight) / 2
      - Each letter: font.pixelSize = letterHeight * 1.1, bold
      - Key learning: PlasmaComponents.Label baseline property returns QQuickAnchorLine()
        (unusable), and Qt.font({...}) returns undefined in Qt6. Set font props directly.
- [X] Month header: always visible in toolbar, updates on scroll — WORKS
      - Removed inline section delegates entirely
      - Toolbar always shows eventList.currentSection
      - updateSection() uses indexAt(); returns early on idx < 0 to prevent flicker
- [X] Dropped window opacity feature (not needed)

In progress — pick up here:

- [ ] Drag-to-move: BREAKTHROUGH — onPressed DOES fire now!
      - Previous "never fires" was likely caused by the Qt.font() error breaking the component
      - Window.window returns DesktopView (a real window object)
      - Current code has debug console.warn lines — remove after confirming drag works
      - FenceWidget approach (for reference): does NOT use Window.window, instead directly
        sets fence.x/fence.y using mapToItem(fence.parent, ...) delta calculations
      - NEXT STEP: just test if the current Window.window approach actually moves the widget
        now that onPressed fires. If DesktopView.x/y can't be set, switch to FenceWidget's
        approach (mapToItem + direct x/y on the applet item)
      - Debug lines still in main.qml onPressed — clean up after confirming

## Testing Checklist

Run `~/dev/ClockEvent/dtest.sh` to deploy, then:

- [ ] Verify clock still displays correctly (time + date)
- [ ] Right-click > Configure opens settings dialog
- [ ] Calendars tab: "Add Calendar" button works, can type label and paste iCal URL
- [ ] Calendars tab: enable/disable checkbox works, delete button works
- [ ] Calendars tab: hit Apply, verify events load (check logs/test.log for "Loaded X events")
- [ ] Appearance tab: 12h/24h toggle works, days ahead spinner works
- [ ] General tab: refresh interval spinner works
- [ ] Events display with month headers (e.g., "March 2026")
- [ ] Date badges show on left (day-of-week + day number), grouped for same-date events
- [ ] Event blocks show title (bold) + time range
- [ ] All-day events show "All day" instead of time range
- [ ] Scrolling works when many events are loaded
- [ ] Clicking an event opens Google Calendar day view in browser
- [ ] Clicking [+] button opens Google Calendar new event page in browser
- [ ] Recurring events (weekly meetings, etc.) expand correctly
- [ ] Check logs/test.log for any QML errors or fetch failures

To get an iCal URL: Google Calendar > Settings > (click a calendar) > "Secret address in iCal format" > Copy

## Polish + Packaging

### Easy
- [ ] Add right-click manual refresh action
- [ ] Update README with install instructions

### Medium
- [ ] Resize behavior (widget adapts, list scrolls)
- [ ] Auto-refresh on configurable interval

### Verification
- [ ] End-to-end test with real iCal feeds on desktop
- [ ] Package and install via kpackagetool6

## Future / Out of Scope

- [ ] Google Calendar API integration (OAuth) for event colors
- [ ] Per-calendar / per-event color coding
- [ ] CalDAV support for non-Google calendars
- [ ] Desktop notifications for upcoming events

## Finished

- [X] Design document
- [X] README.md
- [X] Implementation plan
- [X] Git repo initialized
- [X] Phase 1: Scaffolding + Clock (metadata.json, config, main.qml, ClockSection.qml)
- [X] Phase 2: iCal Fetcher + Parser (icalparser.js, fetcher.js, wired into main.qml)
- [X] Phase 3: Event List UI (EventList.qml, EventRow.qml, MonthHeader.qml)
- [X] Phase 4: Configuration Dialog (ConfigCalendars.qml, ConfigAppearance.qml, ConfigGeneral.qml)
- [X] Phase 5: Click actions (event opens browser, [+] opens new event page)
- [X] Scrollbar styling and positioning (6px custom pill, 9px right gap)
- [X] Overscroll bounce fix
- [X] Sticky month header in toolbar (shows on scroll)
- [X] Timezone vertical letters beside AM/PM (split labels + TextMetrics)
- [X] Month header always in toolbar (no inline section delegates)
- [X] Window opacity feature dropped
